<%# TODO: create custom form builder to put error messages next to each field  %>
<% if f.object.errors.any? %>
  <ul>
    <% f.object.errors.full_messages.each do |message| %>
      <li class="mt-2 text-sm text-red-600"><%= message %></li>
    <% end %>
  </ul>
<% end %>
